# TODO

* build a tkinter gui that displays the title keys from the titles dictionary from recordings.recordedTitles function.
* the interface should show 
    * the appname and version in the title bar
    * a list box containing the titles of shows
    * a quit button

* when a row is selected in the list box, a secondary window should open with
    * the title of the show as the window title
    * a radio selector for Film, Drama or Comedey
    * a list box showing each individual episode in the title 
    * Move and Cancel buttons
    * when Cancel is pressed the window closes and we should return to the main window
    * when Move is pressed the commands to move each episode of the title will be ouptut to the CLI and we return to the main window.