# TODO

* build a dictionary of recorded show titles, removing any in the ignore list. The value of each title is a list of each individual show matching that title.
* step through each title asking the user to (I)gnore it, (S)ave it to Kodi, (Q)uit.